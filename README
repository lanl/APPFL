ABSTRACT

APPFL is a pure functional programming language compiler and runtime
system.  More specifically, the language syntax is a subset of Haskell
but the semantics support varying degrees of strictness necessary for
automatic parallelization.

This code is unclassified and has been assigned LA-CC-15-073.