three = CON(I 3);
four = CON(I 4);

main = THUNK(plusInt three four);

plusInt = FUN(x y ->
   case x of { 
      I i -> case y of { 
               I j -> case plus# i j of { 
                         x -> let { result = CON (I x) } in result }}});
