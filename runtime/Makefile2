CC=gcc
CFLAGS=-c -std=gnu99 -Wall -I/usr/include/malloc
LDFLAGS=
HEADERS=stg.h cmm.h stgmain.h stgcmm.h stgutils.h gc.h
SOURCES=stg.c cmm.c stgcmm.c stgutils.c gc.c
OBJECTS=$(SOURCES:.c=.o)
LIB=libruntime.a
ALL=$(LIB) mymain

UNAME := $(shell uname)
ifeq ($(UNAME), Linux)
CFLAGS+=-Wno-unused-but-set-variable 
else
CFLAGS+=-Wno-unused-const-variable 
endif


all: $(ALL) 

$(LIB) : $(OBJECTS)
	ar -rcs $(LIB) $(OBJECTS)

mymain: mymain.o stgmain.o $(LIB)
	$(CC) $(LDFLAGS) $(OBJECTS) mymain.o stgmain.o -o $@ -L. -lruntime

%.o: %.c $(HEADERS)
	$(CC) $(CFLAGS) $< -o $@

clean:
	rm -f *.o $(ALL)
