! with 2 x's (and a comment!)
one = CON(I 1);
two = CON(I 2);
plusInt = FUN(x y ->
    case x of {
        I i -> case y of {
            I j -> case plus# i j of {
                x -> let { result = CON (I x) } in result }}});
main = THUNK(plusInt one two);
