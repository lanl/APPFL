#include "stg.h"
#include "cmm.h"
#include "stgutils.h"
#include "stgApply.h"
#include <stdlib.h>
#include <stdio.h>

DEFUN0(stgApplyN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 1;
  fprintf(stderr, "stgApplyN %s\n", getInfoPtr(argv[0].op)->name);
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args not possible
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0400000000000000UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 1 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 1);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args not possible
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0400000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0400000000000000UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 1 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 1);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 1;
  fprintf(stderr, "stgApplyP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args not possible
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0400000000000001UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 1 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 1);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args not possible
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0400000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0400000000000001UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 1 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 1);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 2;
  fprintf(stderr, "stgApplyNN %s\n", getInfoPtr(argv[0].op)->name);
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNN FUN too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply FUN 1 excess args\n");
      // arity args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
      // call-with-return the FUN
      fprintf(stderr, "stgApplyNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      fprintf(stderr, "stgApplyNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      argv[0] = stgCurVal;
      // excess args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
             &argv[1+arity],
             excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply
      // jump means replace stgApply stack frame with new
      stgJumpAdjust();
      STGJUMP0(stgApplyN);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x0C00000000000001UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0800000000000000UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 2 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNN PAP too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply PAP to 1 excess args\n");
      // stash excess args
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0400000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
      newframe->layout = bitmap;
      newframe->payload[0] = argv[0]; // self
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // call-with-return the FUN-oid
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      // stash the FUN-oid
      argv[0] = stgCurVal;
      newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      // fun-oid
      newframe->payload[0] = argv[0];
      memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply 
      stgJumpAdjust();
      STGJUMP0(stgApplyN);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0800000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0800000000000000UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 2 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 2;
  fprintf(stderr, "stgApplyPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPN FUN too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply FUN 1 excess args\n");
      // arity args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
      // call-with-return the FUN
      fprintf(stderr, "stgApplyPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      fprintf(stderr, "stgApplyPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      argv[0] = stgCurVal;
      // excess args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
             &argv[1+arity],
             excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply
      // jump means replace stgApply stack frame with new
      stgJumpAdjust();
      STGJUMP0(stgApplyN);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x0C00000000000003UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0800000000000001UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 2 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPN PAP too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply PAP to 1 excess args\n");
      // stash excess args
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0400000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
      newframe->layout = bitmap;
      newframe->payload[0] = argv[0]; // self
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // call-with-return the FUN-oid
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      // stash the FUN-oid
      argv[0] = stgCurVal;
      newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      // fun-oid
      newframe->payload[0] = argv[0];
      memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply 
      stgJumpAdjust();
      STGJUMP0(stgApplyN);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0800000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0800000000000001UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 2 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 2;
  fprintf(stderr, "stgApplyNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNP FUN too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply FUN 1 excess args\n");
      // arity args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
      newframe->layout = (Bitmap64)0x0800000000000001UL;
      memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
      // call-with-return the FUN
      fprintf(stderr, "stgApplyNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      fprintf(stderr, "stgApplyNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      argv[0] = stgCurVal;
      // excess args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
             &argv[1+arity],
             excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply
      // jump means replace stgApply stack frame with new
      stgJumpAdjust();
      STGJUMP0(stgApplyP);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x0C00000000000005UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0800000000000002UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 2 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNP PAP too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply PAP to 1 excess args\n");
      // stash excess args
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0400000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
      newframe->layout = bitmap;
      newframe->payload[0] = argv[0]; // self
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // call-with-return the FUN-oid
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      // stash the FUN-oid
      argv[0] = stgCurVal;
      newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      // fun-oid
      newframe->payload[0] = argv[0];
      memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply 
      stgJumpAdjust();
      STGJUMP0(stgApplyP);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0800000000000002UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0800000000000002UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 2 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 2;
  fprintf(stderr, "stgApplyPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPP FUN too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply FUN 1 excess args\n");
      // arity args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
      // call-with-return the FUN
      fprintf(stderr, "stgApplyPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      fprintf(stderr, "stgApplyPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
      argv[0] = stgCurVal;
      // excess args
      newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
             &argv[1+arity],
             excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply
      // jump means replace stgApply stack frame with new
      stgJumpAdjust();
      STGJUMP0(stgApplyP);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x0C00000000000007UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0800000000000003UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 2 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPP PAP too many args\n");
      // excess can only be 1
      fprintf(stderr, "stgApply PAP to 1 excess args\n");
      // stash excess args
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0400000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
      newframe->layout = bitmap;
      newframe->payload[0] = argv[0]; // self
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // call-with-return the FUN-oid
      STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
      // stash the FUN-oid
      argv[0] = stgCurVal;
      newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
      newframe->layout = (Bitmap64)0x0800000000000003UL;
      // fun-oid
      newframe->payload[0] = argv[0];
      memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
      // try again - tail call stgApply 
      stgJumpAdjust();
      STGJUMP0(stgApplyP);
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0800000000000003UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0800000000000003UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 2 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 2);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyNNN %s\n", getInfoPtr(argv[0].op)->name);
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1000000000000001UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000000UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000000UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1000000000000003UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000001UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000001UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1000000000000005UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000002UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000002UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000002UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1000000000000007UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000003UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000003UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000003UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1000000000000009UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000004UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000004UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000004UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x100000000000000BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000005UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000005UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000005UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x100000000000000DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000006UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000006UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000006UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 3;
  fprintf(stderr, "stgApplyPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x100000000000000FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x0C00000000000007UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 3 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x0C00000000000007UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x0C00000000000007UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 3 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 3);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNNNN %s\n", getInfoPtr(argv[0].op)->name);
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000001UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000000UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000000UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000003UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000001UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000001UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000005UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000002UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000002UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000002UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000007UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000003UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000003UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000003UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000009UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000004UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000004UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000004UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x140000000000000BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000005UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000005UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000005UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x140000000000000DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000006UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000006UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000006UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x140000000000000FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000007UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000007UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000007UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000011UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000008UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000008UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000008UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000013UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1000000000000009UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1000000000000009UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1000000000000009UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000015UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x100000000000000AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x100000000000000AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x100000000000000AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000017UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x100000000000000BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x100000000000000BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x100000000000000BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1400000000000019UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x100000000000000CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x100000000000000CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x100000000000000CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x140000000000001BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x100000000000000DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x100000000000000DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x100000000000000DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x140000000000001DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x100000000000000EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x100000000000000EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x100000000000000EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 4;
  fprintf(stderr, "stgApplyPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x140000000000001FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x100000000000000FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 4 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x100000000000000FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x100000000000000FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 4 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 4);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNNNN %s\n", getInfoPtr(argv[0].op)->name);
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000001UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000000UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000000UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000003UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000001UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000001UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000005UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000002UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000002UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000002UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000007UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000003UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000003UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000003UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000009UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000004UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000004UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000004UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000000BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000005UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000005UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000005UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000000DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000006UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000006UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000006UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000000FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000007UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000007UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000007UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000011UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000008UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000008UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000008UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000013UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000009UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000009UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000009UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000015UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000000AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000000AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000000AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000017UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000000BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000000BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000000BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000019UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000000CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000000CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000000CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000001BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000000DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000000DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000000DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000001DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000000EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000000EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000000EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000001FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000000FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000000FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000000FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000021UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000010UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000010UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000010UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000023UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000011UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000011UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000011UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000025UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000012UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000012UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000012UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000027UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000013UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000013UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000013UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000029UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000014UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000014UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000014UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000002BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000015UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000015UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000015UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000002DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000016UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000016UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000016UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000002FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000017UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000017UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000017UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000031UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000018UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000018UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000018UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000033UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1400000000000019UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1400000000000019UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1400000000000019UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000035UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000001AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000001AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000001AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000037UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000001BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000001BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000001BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1800000000000039UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000001CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000001CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000001CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000003BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000001DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000001DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000001DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyNPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000003DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000001EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000001EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000001EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 5;
  fprintf(stderr, "stgApplyPPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x180000000000003FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x140000000000001FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 5 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x140000000000001FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x140000000000001FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 5 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 5);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNNNN %s\n", getInfoPtr(argv[0].op)->name);
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000001UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000000UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000000UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000000UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000003UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000001UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000001UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000001UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000005UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000002UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000002UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000002UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000007UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000003UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000003UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000003UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000009UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000004UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000004UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000004UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000000BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000005UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000005UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000005UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000000DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000006UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000006UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000006UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPNNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPNNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPNNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000000FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPNNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000007UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000007UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPNNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000007UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPNNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPNNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000011UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000008UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000008UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000008UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000013UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000009UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000009UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000009UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000015UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000000AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000000AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000000AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000017UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000000BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000000BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000000BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000019UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000000CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000000CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000000CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000001BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000000DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000000DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000000DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000001DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000000EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000000EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000000EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPPNN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPPNN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPPNN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPNN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPNN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000001FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPPNN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000000FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPNN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPNN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000000FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPPNN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000000FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPPNN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPPNN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000021UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000021UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000010UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000010UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000010UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000010UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000023UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000023UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000011UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000011UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000011UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000011UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000025UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000025UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000012UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000012UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000012UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000012UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000027UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000027UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000013UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000013UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000013UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000013UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000029UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000029UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000014UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000014UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000014UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000014UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000002BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000002BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000015UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000015UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000015UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000015UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000002DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000002DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000016UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000016UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000016UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000016UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPNPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPNPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPNPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000002FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000002FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPNPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000017UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000017UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000017UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPNPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000017UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPNPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPNPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000031UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000031UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000018UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000018UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000018UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000018UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000033UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000033UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000019UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000019UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000019UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000019UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000035UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000035UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000001AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000001AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000001AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000037UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000037UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000001BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000001BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000001BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000039UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000039UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000001CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000001CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000001CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000003BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000003BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000001DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000001DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000001DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000003DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000003DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000001EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000001EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000001EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPPPN) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPPPN %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPPPN THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPPN FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000003FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPN CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPN back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPPN FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000003FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPPPN FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000001FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPPN PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyN);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPN);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPN);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPN);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPN);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPPN PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000001FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPPPN PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000001FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPPPN!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPPPN not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000021UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000041UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000020UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000021UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000020UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000020UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000021UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000043UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000021UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000021UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000021UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000021UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000023UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000045UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000022UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000023UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000022UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000022UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000023UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000047UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000023UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000023UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000023UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000023UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000025UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000049UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000024UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000025UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000024UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000024UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000025UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000004BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000025UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000025UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000025UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000025UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000027UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000004DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000026UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000027UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000026UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000026UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPNNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPNNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPNNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000027UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000004FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPNNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000027UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000027UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000027UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPNNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000027UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPNNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPNNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000029UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000051UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000028UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000029UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000028UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000028UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000029UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000053UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000029UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000029UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000029UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000029UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000002BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000055UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000002AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000002BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000002AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000002AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000002BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000057UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000002BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000002BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000002BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000002BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000002DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000059UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000002CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000002DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000002CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000002CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000002DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000005BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000002DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000002DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000002DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000002DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000002FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000005DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000002EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000002FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000002EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000002EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPPNP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPPNP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPPNP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPNP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPNP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPNP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000002FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPNP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000005FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPPNP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000002FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPNP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000002FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPNP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPNP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000002FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPPNP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000002FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPPNP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPPNP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000021UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000031UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000061UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000030UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000010UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000031UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000030UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000030UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000023UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000031UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000063UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000031UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000011UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000031UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000031UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000031UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000025UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000033UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000065UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000032UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000012UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000033UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000032UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000032UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000027UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000033UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000067UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000033UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000013UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000033UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000033UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000033UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000029UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000035UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000069UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000034UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000014UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000035UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000034UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000034UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000002BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000035UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000006BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000035UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000015UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000035UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000035UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000035UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000002DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000037UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000006DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000036UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000016UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000037UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000036UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000036UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPNPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPNPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPNPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000002FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPNPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPNPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000037UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000006FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPNPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000037UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPNPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000017UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000037UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPNPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPNPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000037UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPNPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000037UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPNPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPNPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000031UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000011UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000039UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000071UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000038UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000018UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000008UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000039UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000038UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000038UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000033UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000013UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x1800000000000039UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000073UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x1800000000000039UL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1400000000000019UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x1000000000000009UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x1800000000000039UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x1800000000000039UL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x1800000000000039UL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000035UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000015UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000003BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000075UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000003AUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000AUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000003BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000003AUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000003AUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPNPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPNPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPNPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000037UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000017UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPNPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPNPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000003BUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000077UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPNPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000003BUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPNPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000BUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000003BUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPNPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPNPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000003BUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPNPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000003BUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPNPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPNPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNNPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNNPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNNPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1800000000000039UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1400000000000019UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x1000000000000009UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000003DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C00000000000079UL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNNPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000003CUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNNPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000CUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000004UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000003DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNNPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000003CUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNNPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000003CUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNNPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNNPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPNPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPNPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPNPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000003BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000BUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPNPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPNPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000003DUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000007BUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPNPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000003DUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPNPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000DUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000005UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000003DUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyNPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPNPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000003DUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPNPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000003DUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPNPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPNPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyNPPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyNPPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyNPPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000003DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000DUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000005UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000001UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyNPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyNPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000003FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000007DUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyNPPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000003EUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyNPPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000EUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000006UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000002UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000000UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000003FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyNPPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000003EUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyNPPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000003EUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyNPPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyNPPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

DEFUN0(stgApplyPPPPPP) {
  // STACKCONT with actual parameters
  Cont *argframe = stgGetStackArgp();
  // continuations don't move in GC
  PtrOrLiteral * const argv = argframe->payload;
  // STACKCONT to be constructed for call/jump
  Cont *newframe;
  // pointer to args in argframe
  const int argc = 6;
  fprintf(stderr, "stgApplyPPPPPP %s\n", getInfoPtr(argv[0].op)->name);
  STGEVAL(argv[1]);
  // stgCurVal possibly less indirect
  argv[1] = stgCurVal;
  STGEVAL(argv[2]);
  // stgCurVal possibly less indirect
  argv[2] = stgCurVal;
  STGEVAL(argv[3]);
  // stgCurVal possibly less indirect
  argv[3] = stgCurVal;
  STGEVAL(argv[4]);
  // stgCurVal possibly less indirect
  argv[4] = stgCurVal;
  STGEVAL(argv[5]);
  // stgCurVal possibly less indirect
  argv[5] = stgCurVal;
  STGEVAL(argv[6]);
  // stgCurVal possibly less indirect
  argv[6] = stgCurVal;
  argv[0].op = derefPoL(argv[0]);
  // this if just saves a possibly unneeded call
  if (getObjType(argv[0].op) == THUNK) {
    fprintf(stderr, "stgApplyPPPPPP THUNK\n");
    // return through top-of-stack STACKCONT
    STGEVAL(argv[0]);
    // this works because stgCurVal is a GC root
    argv[0].op = derefPoL(stgCurVal);
  } // if THUNK
  
  switch (getObjType(argv[0].op)) {
  
  case FUN: {
    int arity = getInfoPtr(argv[0].op)->funFields.arity;
    fprintf(stderr, "FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;  // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPPP FUN too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply FUN 1 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x180000000000003FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply FUN 2 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply FUN 3 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply FUN 4 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply FUN 5 excess args\n");
        // arity args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+arity );
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        memcpy(newframe->payload, argv, (1+arity) * sizeof(PtrOrLiteral));
        // call-with-return the FUN
        fprintf(stderr, "stgApplyPPPPPP CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        fprintf(stderr, "stgApplyPPPPPP back from CALLing  %s\n", getInfoPtr(argv[0].op)->name);
        argv[0] = stgCurVal;
        // excess args
        newframe = stgAllocStackCont( &it_stgStackCont, 1+excess );
        newframe->layout = (Bitmap64)0x180000000000003FUL;
        newframe->payload[0] = argv[0];memcpy(&newframe->payload[1],
               &argv[1+arity],
               excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply
        // jump means replace stgApply stack frame with new
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPPP FUN just right\n");
      newframe = stgAllocStackCont( &it_stgStackCont, argc+1 );
      newframe->layout = (Bitmap64)0x1C0000000000007FUL;
      memcpy(newframe->payload, argv, (argc+1) * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
    }
    // excess < 0, too few args
    else {
      fprintf(stderr, "stgApplyPPPPPP FUN too few args\n");
      int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                    getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
      // stgNewHeapPAPmask puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), (Bitmap64)0x180000000000003FUL);
      // copy fvs
      fprintf(stderr, "stgApply FUN inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy args to just after fvs and layout info
      fprintf(stderr, "stgApply FUN inserting 6 args into new PAP\n");
      copyargs(&pap->payload[fvCount+1], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      // pop stgApply cont
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case FUN
  
  
  case PAP: {
    int fvCount = getInfoPtr(argv[0].op)->layoutInfo.boxedCount + 
                  getInfoPtr(argv[0].op)->layoutInfo.unboxedCount;
    Bitmap64 bitmap = argv[0].op->payload[fvCount].b;
    Bitmap64 bitmap2;
    int argCount = BMSIZE(bitmap);
    int arity = getInfoPtr(argv[0].op)->funFields.arity - argCount;
    fprintf(stderr, "PAP/FUN %s arity %d\n", getInfoPtr(argv[0].op)->name, getInfoPtr(argv[0].op)->funFields.arity);
    int excess = argc - arity;    // may be negative
  
    // too many args?
    if (excess > 0) {
      fprintf(stderr, "stgApplyPPPPPP PAP too many args\n");
      switch (excess) {
      case 1: {
        fprintf(stderr, "stgApply PAP to 1 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x140000000000001FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0800000000000003UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyP);
        break;
      } // case 1
      case 2: {
        fprintf(stderr, "stgApply PAP to 2 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x100000000000000FUL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x0C00000000000007UL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPP);
        break;
      } // case 2
      case 3: {
        fprintf(stderr, "stgApply PAP to 3 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0C00000000000007UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x100000000000000FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPP);
        break;
      } // case 3
      case 4: {
        fprintf(stderr, "stgApply PAP to 4 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0800000000000003UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x140000000000001FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPP);
        break;
      } // case 4
      case 5: {
        fprintf(stderr, "stgApply PAP to 5 excess args\n");
        // stash excess args
        bitmap.bitmap.mask <<= 1;
        bitmap.bitmap.mask |= 0x1;
        bitmap.bitmap.size += 1;
        bitmap2 = (Bitmap64)0x0400000000000001UL;
        bitmap2.bitmap.mask <<= (argCount + 1);
        bitmap.bits += bitmap2.bits;
        newframe = stgAllocStackCont( &it_stgStackCont, 1+argCount+arity );
        newframe->layout = bitmap;
        newframe->payload[0] = argv[0]; // self
        memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral)); // old args
        memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
        // call-with-return the FUN-oid
        STGCALL0(getInfoPtr(argv[0].op)->funFields.trueEntryCode);
        // stash the FUN-oid
        argv[0] = stgCurVal;
        newframe = stgAllocStackCont(&it_stgStackCont, 1 + excess);
        newframe->layout = (Bitmap64)0x180000000000003FUL;
        // fun-oid
        newframe->payload[0] = argv[0];
        memcpy(&newframe->payload[1], &argv[1+arity], excess * sizeof(PtrOrLiteral));
        // try again - tail call stgApply 
        stgJumpAdjust();
        STGJUMP0(stgApplyPPPPP);
        break;
      } // case 5
        default: fprintf(stderr, "switch on excess reached default!\n"); exit(0);
      } // switch(excess)
    } else 
  
    // just right?
    if (excess == 0) {
      fprintf(stderr, "stgApplyPPPPPP PAP just right\n");
      // push new args
      fprintf(stderr, "PAP just right:  %d args in PAP, %d new args\n", argCount, arity);
      bitmap.bitmap.mask <<= 1;
      bitmap.bitmap.mask |= 0x1;
      bitmap.bitmap.size += 1;
      bitmap2 = (Bitmap64)0x180000000000003FUL;
      bitmap2.bitmap.mask <<= (argCount + 1);
      bitmap.bits += bitmap2.bits;
      newframe = stgAllocStackCont( &it_stgStackCont, argCount+1+arity );
      newframe->layout = bitmap;
      // self
      newframe->payload[0] = argv[0];
      // old args
      memcpy(&newframe->payload[1], &argv[0].op->payload[1 + fvCount], argCount * sizeof(PtrOrLiteral));
      // new args
      memcpy(&newframe->payload[1 + argCount], &argv[1], arity * sizeof(PtrOrLiteral));
      // stgJumpAdjust invalidates argv and newframe
      newframe = stgJumpAdjust();
      // tail call the FUN
      STGJUMP0(getInfoPtr(newframe->payload[0].op)->funFields.trueEntryCode);
  
    // excess < 0, too few args
    } else {
      fprintf(stderr, "stgApplyPPPPPP PAP too few args\n");
      // bitmap for new args
      Bitmap64 bmold = argv[0].op->payload[fvCount].b;
      Bitmap64 bmnew = (Bitmap64)0x180000000000003FUL;
      // shift mask by known only at runtime #existing PAP args
      bmnew.bitmap.mask <<= argCount;
      bmnew.bits += bmold.bits;
      // stgNewHeapPAP puts layout info at payload[fvCount]
      Obj *pap = stgNewHeapPAPmask(getInfoPtr(argv[0].op), bmnew);
      // copy fvs
      fprintf(stderr, "stgApply PAP inserting %d FVs into new PAP\n", fvCount);
      copyargs(&pap->payload[0], &argv[0].op->payload[0], fvCount);
      // copy old args
      fprintf(stderr, "stgApply PAP inserting %d old args into new PAP\n", argCount);
      copyargs(&pap->payload[fvCount+1], &argv[0].op->payload[fvCount+1], argCount);
      // copy new args to just after fvs, layout info, and old args
      fprintf(stderr, "stgApply PAP inserting 6 new args into new PAP\n");
      copyargs(&pap->payload[fvCount+1+argCount], &argv[1], 6);
      stgCurVal = HOTOPL(pap);
      stgPopCont();
      STGRETURN0();
    } // if excess
  } // case PAP
  
  case BLACKHOLE: {
    fprintf(stderr, "infinite loop detected in stgApplyPPPPPP!\n");
    showStgHeap();
    assert(0);
  } // case BLACKHOLE
  
  default:
    fprintf(stderr, "stgApplyPPPPPP not a THUNK, FUN, or PAP\n");
    exit(0);
  }  // switch
  ENDFUN;
}

